<h1>Create a new signup </h1>

<%= form_for @signup do |f|%>

<%= f.label :camper_id %>
<%= f.collection_select :camper_id, Camper.all, :id, :name %><br><br>

<%= f.label :activity_id %>
<%= f.collection_select :activity_id, Activity.all, :id, :name %><br><br>

<%= f.label :time %>
<%= f.select :time, (0..23) %><br><br>

<%= f.submit %>
<%end%>